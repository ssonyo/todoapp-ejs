<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/post/list">TodoApp</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="navbar-nav">
        <a class="nav-link" href="/post/list">목록</a>
        <a class="nav-link" href="/post/add">글쓰기</a>
      </div>

      <div class="navbar-nav ms-auto align-items-lg-center">
        <% if (user) { %>
          <span class="navbar-text me-lg-3 mb-2 mb-lg-0">
            안녕하세요, <strong><%= user.username %></strong>님!
          </span>
          <div>
            <a class="btn btn-outline-danger btn-sm" href="/auth/logout">로그아웃</a>
          </div>
        <% } else { %>
          <a class="nav-link" href="/auth/login">로그인</a>
          <a class="nav-link" href="/auth/register">회원가입</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>