<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>상세페이지</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light"> <%- include('nav.ejs') %> <div class="container mt-5"> <div class="d-flex justify-content-between align-items-center mb-4">
    <div class="container mt-5">
        <div class="card shadow-sm"> <div class="card-header bg-primary text-white">
                상세 정보
            </div>
            <div class="card-body">
                <h5 class="card-title fw-bold"><%= data.title %></h5>
                
                <div class="row mt-3 mb-3">
                    <div class="col-sm-6">
                        <p class="text-muted mb-1" style="font-size: 0.9rem;">📅 작성일</p>
                        <p><%= data.createdAt ? new Date(data.createdAt).toLocaleString('ko-KR') : '정보 없음' %></p>
                    </div>
                    <div class="col-sm-6">
                        <p class="text-muted mb-1" style="font-size: 0.9rem;">⏰ 마감기한</p>
                        <p class="fw-bold text-danger">
                            <%= data.dueDate ? data.dueDate : 'Undefined' %>
                        </p>
                    </div>
                </div>

                <hr>
                <p class="card-text" style="white-space: pre-line;"><%= data.content %></p>
                
                <div class="mt-4 border-top pt-3">
                    <a href="/list" class="btn btn-outline-secondary">목록보기</a>
                    <a href="/edit/<%= data._id %>" class="btn btn-warning">수정하기</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>